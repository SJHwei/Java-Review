package com.company.反射机制.反射Field.通过反射机制访问对象属性;

/**
 * @author ShiWei
 * @date 2021/2/3 - 10:55
 *
 * 1. 怎么样通过反射机制访问一个java对象的属性?
 *    (1) 给属性赋值: set(类对象, 值);
 *    (2) 获取属性的值: get(类对象).
 *
 * 2. 不管是否使用反射机制, 给一个对象的属性赋值都要满足三要素:
 *    (1) 要素一: 对象;
 *    (2) 要素二: 属性;
 *    (3) 要素三: 值.
 *
 * 3. 不管是否使用反射机制, 获取一个对象的属性的值都要满足二要素:
 *    (1) 要素一: 对象;
 *    (2) 要素二: 属性.
 */
public class 通过反射机制访问对象属性 {
}
