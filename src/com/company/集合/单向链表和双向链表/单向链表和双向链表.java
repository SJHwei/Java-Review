package com.company.集合.单向链表和双向链表;

/**
 * 1. 单向链表
 *    (1) 定义: 单向链表每个元素都称作节点(entry), 每个节点都由两部分组成.
 *    (2) 缺点: 查询效率低.
 *              单向链表中的每个元素在空间的存储位置上没有规律, 也没有顺序, 那么在查找某个元素的时候必须从头结点挨着往后找, 直到找到为止.
 *    (3) 优点: 增删效率高.
 *              因为链表每个元素存储的空间是没有顺序的, 删除或者添加某个元素, 只需要让指针重新指向即可. 不需要将其他元素位移. 所以随机增删效率较高.
 *    (4) 注意: 单向链表每个节点在内存中存储上在空间位置上是无规律的.
 *
 * 2. 双向链表:
 *    是个环状. 节点由三部分组成.
 * */

public class 单向链表和双向链表 {

}
