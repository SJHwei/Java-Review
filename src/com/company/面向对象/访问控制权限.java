package com.company.面向对象;

/**
 * 1. 四种访问控制权限修饰符: private, default, protected, public.
 *
 * 2. 访问控制权限修饰符可以修饰三种东西: 类, 方法, 变量.
 *
 * 3. 四种访问控制权限修饰符限制的访问范围:
 *   (1) private: 只能在本类中访问;
 *   (2) default: 可以在本类中, 同一个包下访问, 在不同包下不能访问;
 *   (3) protected: 可以在本类中, 同一个包下访问, 在不同包下不能访问, 但是子类中可以;
 *   (4) public: 可以在任何位置访问.
 *
 * 4. 重点:
 *   (1) 类只能使用public或缺省方式.
 *   (2) 四种访问控制权限修饰符的权限从小到大排序: private < default < protected < public.
 *
 * */

public class 访问控制权限 {
}
